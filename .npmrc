# Pin this repo to the public npm registry (overrides any global CFA Artifactory
# default in ~/.npmrc). The extension's deps (typebox, pi packages) are public;
# resolving them through a private proxy bakes authenticated URLs into bun.lock
# and breaks `bun install` in CI (and for anyone without CFA creds).
registry=https://registry.npmjs.org/
